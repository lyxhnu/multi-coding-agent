# APP Workspace

This directory stores generated application projects created by the multi-agent coding system.

- Each new coding run gets its own subfolder under `APP/`.
- Run metadata, logs, and handoff files still live under `backend/workspace/runs/`.
- The host repository should remain clean from generated app code.
